<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>익명계시판</title>
  <link rel="stylesheet" href="/static/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <div class="head">
      <h2>관리자 페이지</h2>
    </div>
    <table class="table table-striped table-inverse">
      <thead>
        <tr>
          <th>번호</th>
          <th>게시물</th>
          <th>작성자</th>
          <th>삭제</th>
        </tr>
      </thead>
      <tbody>
      <a href="/" class="btn btn-outline-success">목록</a>
      <% data.forEach(item => { %>
        <form action="/admin/<%= item.num %>" method="post" class="text-right">
        <tr>
          <td><%= item.num %></td>
          <td><a href="/content/<%= item.num %>"><%= item.title %></td>
          <td><%= item.author %></td>
          <td>
              <input type="submit" class="btn btn-outline-danger" value="삭제">
          </td>
        </tr>
        </form>
      <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>